[build-system]
requires = []
build-backend = "setuptools.build_meta"

[project]
name = "sms-gateway"
version = "0.1.0"
description = "Durable home SMS gateway MVP"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
